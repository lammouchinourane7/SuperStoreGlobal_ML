services:
  - type: web
    name: superstore-ml-app
    runtime: python
    rootDir: ML/superstore-app
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: SECRET_KEY
        generateValue: true
